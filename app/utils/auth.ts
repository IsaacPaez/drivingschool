// Archivo vacío: autenticación eliminada
export function useVerifySession(): boolean {
  // Aquí deberías poner la lógica real de verificación de sesión
  // Por ahora, retorna true para que no falle el código
  return true;
}